/**
 * Main entry point for CLI testing (deprecated - use web UI instead)
 * See main.ts for the web application
 */
function main() {
  console.log('CLI testing is deprecated. Please use the web UI');
  console.log('The application runs in the browser via index.html -> main.ts');
}

export { main };
